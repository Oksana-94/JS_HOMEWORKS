'use strict';

const add = function(a, b) {
    return a + b;
}

const sub = function(a, b) {
    return a - b;
}

const mul = function(a, b) {
    return a * b;
}

console.log(add(4, 3));
console.log(sub(4, 3));
console.log(mul(4, 3));
